version: '2'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.pi
    environment:
      FLASK_APP: 'src/web.py'
      FLASK_DEBUG: 1
    command: 'python3 -m flask run --host=0.0.0.0 --port=80'
    network_mode: 'host'
    volumes:
    - .:/app
    ports:
    - 80
